import { Router, RouterLink  } from '@angular/router';
import { MatCardModule } from '@angular/material/card';
import { MatFormFieldModule } from '@angular/material/form-field';
import { MatInputModule } from '@angular/material/input';
import { MatButtonModule } from '@angular/material/button';
import { MatIconModule } from '@angular/material/icon';
import { AuthService } from '../../core/services/auth.service';



@Component({
  selector: 'app-login',
  standalone: true,
  imports: [
    CommonModule,
    ReactiveFormsModule,
    RouterLink,
    MatCardModule,
    MatFormFieldModule,
    MatInputModule,
    MatButtonModule,
    MatIconModule
  ],
  template: `
    <div class="login-wrapper">
       <div class="glass-board">
        <section class="brand-panel">
          <div class="brand-icon">
            <mat-icon>account_tree</mat-icon>
          </div>
          <h2>BPMN STUDIO</h2>
          <p>Transform your business<br />processes with AI</p>
        </section>

        <mat-card class="login-card">
          <div class="title-wrap">
            <span class="pill">Welcome back</span>
            <h1>Sign in</h1>
            <p>Use one of the mock Bouygues Telecom accounts.</p>
          </div>
          <form [formGroup]="loginForm" (ngSubmit)="submit()">
            <mat-form-field appearance="outline">
              <mat-label>Email*</mat-label>
              <input matInput type="email" formControlName="email" />
              <mat-icon matSuffix>mail</mat-icon>
            </mat-form-field>

            <mat-form-field appearance="outline">
              <mat-label>Password*</mat-label>
              <input matInput type="password" formControlName="password" />
              <mat-icon matSuffix>lock</mat-icon>
            </mat-form-field>

            <button mat-flat-button color="primary" type="submit" [disabled]="loginForm.invalid">
              Login
            </button>

            <p class="error" *ngIf="errorMessage">{{ errorMessage }}</p>
          </form>

          <div class="register-link">
            <p>New to BPMN Studio? <a routerLink="/register">Create an account</a></p>
          </div>

          <div class="accounts">
            <p><strong>Admin:</strong> admin&#64;bouygues.com / admin123</p>
            <p><strong>User:</strong> user&#64;bouygues.com / user123</p>
          </div>
        </mat-card>
      </div> 
    </div>
  `,
  styles: [
    `
      .login-wrapper {
        min-height: calc(100vh - 80px);
        padding: 20px;
        display: grid;
        place-items: center;
        background: linear-gradient(130deg, #d4d7ef 0%, #c2d8e9 100%);
      }

      .glass-board {
         width: min(1400px, 100%);
        border-radius: 28px;
        border: 2px solid rgba(255, 255, 255, 0.55);
        display: grid;
        grid-template-columns: minmax(300px, 1fr) minmax(420px, 1.1fr);
        overflow: hidden;
        background:
          linear-gradient(120deg, rgba(99, 102, 241, 0.46) 0%, rgba(190, 219, 241, 0.58) 62%, rgba(218, 232, 243, 0.58) 100%),
          radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.32), transparent 58%);
        box-shadow: 0 22px 60px rgba(39, 54, 101, 0.2);
      }

      .brand-panel {
         min-height: 680px;
        display: grid;
        align-content: center;
        justify-items: center;
        gap: 20px;
        text-align: center;
        color: #edf3ff;
        padding: 40px;
        background:
          radial-gradient(circle at 20% 25%, rgba(255, 255, 255, 0.22), transparent 45%),
          linear-gradient(165deg, rgba(90, 108, 238, 0.9) 0%, rgba(66, 89, 220, 0.88) 55%, rgba(78, 111, 238, 0.6) 100%);
      }

      .brand-icon {
        width: 138px;
        height: 138px;
        border-radius: 26px;
        display: grid;
        place-items: center;
        background: linear-gradient(145deg, #f4f8ff 0%, #b7ccff 100%);
        box-shadow: 0 20px 34px rgba(30, 42, 84, 0.3);
      }

      .brand-icon mat-icon {
        font-size: 76px;
        width: 76px;
        height: 76px;
        color: #4e64e8;
      }

      .brand-panel h2 {
        margin: 8px 0 0;
        font-size: clamp(34px, 4vw, 62px);
        font-weight: 700;
        letter-spacing: 0.04em;
      }

      .brand-panel p {
        margin: 0;
        font-size: clamp(20px, 2vw, 46px);
        line-height: 1.25;
        color: #e8f0ff;
        font-weight: 400;
      }

      .login-card {
        width: 100%;
        margin: 52px;
        border-radius: 34px;
        padding: 42px 50px;
        background: rgba(255, 255, 255, 0.9);
        border: 2px solid rgba(232, 235, 255, 0.9);
        box-shadow: 0 20px 44px rgba(68, 78, 122, 0.15);
      }

      .title-wrap {
        margin-bottom: 14px;
      }

      .pill {
        display: inline-block;
        border-radius: 999px;
        padding: 10px 20px;
        margin-bottom: 14px;
        font-size: 37px;
        color: #6355dd;
        background: #edeafe;
      }

      h1 {
        margin: 0;
        font-size: clamp(58px, 5vw, 90px);
        line-height: 1.1;
        color: #253056;
      }

      p {
        margin: 16px 0 0;
        color: #5b6689;
        font-size: clamp(22px, 1.55vw, 37px);
      }

      form {
        display: grid;
        gap: 18px;
        margin: 26px 0 10px;
      }

      mat-form-field {
        font-size: clamp(22px, 1.4vw, 30px);
      }

      :host ::ng-deep .mat-mdc-form-field-subscript-wrapper {
        display: none;
      }

      :host ::ng-deep .mat-mdc-text-field-wrapper {
        border-radius: 10px !important;
        min-height: 74px;
      }

      :host ::ng-deep .mat-mdc-input-element {
        font-size: clamp(30px, 2vw, 42px) !important;
      }

      :host ::ng-deep .mat-icon {
        transform: scale(1.3);
      }

      .register-link {
        text-align: center;
        margin: 26px 0;
      }

      .register-link p {
        font-size: clamp(22px, 1.5vw, 35px);
      }

      .register-link a {
        color: #594be0;
        text-decoration: none;
        font-weight: 600;
      }

      .register-link a:hover {
        text-decoration: underline;
      }

      button {
        border-radius: 24px;
        min-height: 70px;
        font-weight: 600;
        font-size: clamp(26px, 1.5vw, 34px);
        margin-top: 10px;
        background: linear-gradient(90deg, #4c48db 0%, #9954e8 100%);
      }

      .error {
        color: #d32f2f;
        margin: 0;
      }

      .accounts {
        margin-top: 14px;
        padding-top: 22px;
        border-top: 1px solid #e2e8f5;

        font-size: clamp(20px, 1.3vw, 31px);
        color: #5d6a7c;
      }

      .accounts p {
        margin-top: 8px;
      }

      @media (max-width: 1080px) {
        .glass-board {
          grid-template-columns: 1fr;
        }

        .brand-panel {
          min-height: 220px;
          gap: 12px;
          padding: 28px;
        }

        .brand-icon {
          width: 72px;
          height: 72px;
          border-radius: 16px;
        }

        .brand-icon mat-icon {
          font-size: 42px;
          width: 42px;
          height: 42px;
        }

        .brand-panel h2 {
          font-size: clamp(24px, 4vw, 38px);
        }

        .brand-panel p {
          font-size: clamp(16px, 3vw, 22px);
        }

        .login-card {
          margin: 0;
          border-radius: 0;
          border: 0;
          box-shadow: none;
        }
      }
    `
  ]
})
export class LoginComponent {
  private readonly fb = inject(FormBuilder);
  private readonly authService = inject(AuthService);
  private readonly router = inject(Router);
  

  errorMessage = '';

  readonly loginForm = this.fb.nonNullable.group({
    email: ['', [Validators.required, Validators.email]],
    password: ['', [Validators.required, Validators.minLength(6)]]
  });

submit(): void {
  const { email, password } = this.loginForm.getRawValue();
  this.authService.login(email, password).subscribe({
    next: () => {
      this.errorMessage = '';
      this.router.navigate(['/home']);
    },
    error: (err) => {
      this.errorMessage = 'Invalid credentials. Please use the mock accounts.';
    }
  })
}
}